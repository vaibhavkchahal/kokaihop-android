<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:bind="http://schemas.android.com/tools">

    <data>
        <variable name="viewModel" type="com.kokaihop.recipedetail.RecipeDetailViewModel"/>
        <variable name="profileImageUrl" type="String"/>
    </data>

    <android.support.design.widget.CoordinatorLayout
        android:id="@+id/container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fitsSystemWindows="true">

        <android.support.design.widget.AppBarLayout
            android:id="@+id/appbarLayout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/white"
            android:fitsSystemWindows="true">


            <android.support.design.widget.CollapsingToolbarLayout
                android:id="@+id/collapsing_toolbarLayout"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                app:layout_scrollFlags="scroll|exitUntilCollapsed|snap">

                <RelativeLayout android:id="@+id/relative_layout_pager"
                                android:layout_width="match_parent"
                                android:layout_height="280dp"
                                app:layout_collapseMode="parallax">

                    <android.support.v4.view.ViewPager
                        android:id="@+id/viewpager_recipe_detail"
                        android:layout_width="match_parent"
                        android:layout_height="match_parent"
                        android:background="@color/transparent_color"
                        app:layout_behavior="@string/appbar_scrolling_view_behavior"/>

                    <View android:layout_width="match_parent" android:layout_height="match_parent"
                          android:background="@drawable/recipe_detail_gradient"

                    />

                    <ImageView
                        android:id="@+id/viewpager_swipe_left"
                        android:layout_width="28dp"
                        android:layout_height="28dp"
                        android:layout_centerVertical="true"
                        android:layout_gravity="center_vertical|left"
                        android:layout_marginLeft="10dp"
                        android:src="@drawable/ic_swipeleft_md"
                        android:visibility="gone"/>

                    <ImageView
                        android:id="@+id/viewpager_swipe_right"
                        android:layout_width="28dp"
                        android:layout_height="28dp"
                        android:layout_alignParentRight="true"
                        android:layout_centerVertical="true"
                        android:layout_gravity="center_vertical|right"
                        android:layout_marginRight="10dp"
                        android:src="@drawable/ic_swiperight_md"
                        android:visibility="gone"/>

                    <TextView
                        android:id="@+id/txtview_pager_progress"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_alignParentBottom="true"
                        android:layout_marginBottom="17dp"
                        android:layout_marginLeft="20dp"
                        android:textColor="@color/white"
                        app:font="@{`SS-ProRegular`}"
                    />

                    <!--<TextView
                        android:id="@+id/txtview_userprofile"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_alignParentBottom="true"
                        android:layout_alignParentRight="true"
                        android:layout_centerVertical="true"
                        android:layout_marginBottom="17dp"
                        android:layout_marginRight="60dp"
                        android:gravity="center_vertical"
                        android:text="@{viewModel.recipeRealmObject.createdBy.name}"
                        android:textColor="@color/white"
                        android:textSize="15sp"/>

                    <ImageView
                        android:id="@+id/imgview_userprofile"
                        android:layout_width="@dimen/recipe_detail_header_profile_img_height_width"
                        android:layout_height="@dimen/recipe_detail_header_profile_img_height_width"
                        android:layout_alignParentBottom="true"
                        android:layout_alignParentRight="true"
                        android:layout_centerVertical="true"
                        android:layout_marginBottom="10dp"
                        android:layout_marginRight="20dp"
                        app:error="@{@drawable/ic_avtar_sm}"
                        app:imageUrl="@{profileImageUrl}"
                        app:isCircular="@{true}"
                        app:placeholder="@{@drawable/ic_avtar_sm}"/>-->

                    <LinearLayout android:layout_width="wrap_content"
                                  android:layout_height="wrap_content"
                                  android:layout_alignParentBottom="true"
                                  android:layout_alignParentRight="true"
                                  android:layout_marginBottom="15dp"
                                  android:layout_marginRight="10dp"
                                  android:onClick="@{(v)->viewModel.openUserProfile(v)}"
                                  android:gravity="center_vertical">
                        <TextView
                            android:id="@+id/txtview_userprofile"
                            android:layout_width="wrap_content"
                            android:layout_height="wrap_content"
                            android:layout_marginRight="10dp"
                            android:text="@{viewModel.recipeRealmObject.createdBy.name}"
                            android:textColor="@color/white"
                            android:textSize="13sp"
                            app:font="@{`SS-ProRegular`}"/>

                        <ImageView
                            android:id="@+id/imgview_userprofile"
                            android:layout_width="@dimen/recipe_detail_header_profile_img_height_width"
                            android:layout_height="@dimen/recipe_detail_header_profile_img_height_width"
                            app:error="@{@drawable/ic_avtar_sm}"
                            app:imageUrl="@{profileImageUrl}"
                            app:isCircular="@{true}"
                            app:placeholder="@{@drawable/ic_avtar_sm}"/>
                    </LinearLayout>


                </RelativeLayout>

                <android.support.v7.widget.Toolbar
                    android:id="@+id/recipe_detail_toolbar"
                    android:layout_width="match_parent"
                    android:layout_height="?attr/actionBarSize"
                    app:layout_collapseMode="pin">

                    <ImageView
                        android:id="@+id/imgview_back"
                        style="@style/backButton"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"/>

                </android.support.v7.widget.Toolbar>

            </android.support.design.widget.CollapsingToolbarLayout>

        </android.support.design.widget.AppBarLayout>

        <android.support.v7.widget.RecyclerView
            android:id="@+id/recycler_view_recipe_detail"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@color/white"
            app:layout_behavior="@string/appbar_scrolling_view_behavior"/>

        <include
            layout="@layout/progress_bar"
            bind:viewModel="@{viewModel}"/>


    </android.support.design.widget.CoordinatorLayout>
</layout>